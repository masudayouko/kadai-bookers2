
<div class="body">
  <div class="container">
    <p id="notice"></p>
     <div class="row">
       <%= render 'users/a', user: current_user, book: @book %>
<div class="col-xs-9">
<h1>Books</h1>
     <table class="table table-striped">
     <thead>
     <tr>      </tr>
     <th>Title</th>
     <th>Opinion</th>
     <th colspan="3"></th>
     </tr>
     </thead>

     <tbody>
     
     <% @books.each do |book| %>
      <tr>
        <td><%= attachment_image_tag book.user, :profile_image, :fill,40,40, fallback: "no_image.jpg",size: "60x60" %></td>

       <td><%= link_to book.title, user_path(book.user) %><td>
        <td><%= book.body %></td>
      </tr>
     <% end %>
    </tbody>
    </table>
  </div>
</div>
</div>
</div>